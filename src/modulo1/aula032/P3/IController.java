package P3;

public interface IController {
    public String salvar (String dado);
}
    // interface não pode ter um objeto na View
    // a interface é feita para ser implementada, reutilizada, mas não para new

    
